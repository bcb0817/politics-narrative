# Threads公式API 拡張実装計画 🧵

最終確認日: 2026-07-26
対象: Windowsローカル政治ニュースBot「久世ゆい」

## 方針

- `graph.threads.net` のMeta公式APIだけを使用する。
- スクレイピング、Cookie利用、ブラウザ自動操作、非公式APIは使用しない。
- 取得・同期・ローカル分析を先に実装し、外部状態を変える操作は既定で無効にする。
- 返信、引用、リポスト、削除、返信管理、メディア投稿は、環境変数による個別許可とCLIの `--confirm` の両方を必須にする。
- API仕様を確認できない機能は実装せず、機能対応表で「未確認」とする。
- SQLite変更は追加のみ、再実行可能とし、既存履歴を削除しない。

## A. 現在のトークンで利用可能

現在ローカルに記録されているスコープ:

- `threads_basic`
- `threads_content_publish`
- `threads_manage_insights`

実装対象:

- 自分のプロフィールと投稿の取得・同期
- 投稿別・アカウント別Insights
- 投稿コンテナ作成、状態確認、公開
- テキスト、画像、動画、カルーセル、リンク、投票、GIF、テキスト添付、トピックタグ、引用、ゴースト投稿、スポイラー、alt text、reply control
- 公式公開枠の取得
- 長期トークン交換・更新・デバッグ
- ローカル日次/週次レポート、X比較、保存期限管理

外部投稿系は権限があっても既定で無効であり、人間確認なしでは実行しない。

## B. 追加OAuth認証・権限承認後に利用可能

`--scope-profile full-analysis` で再認証URLを生成する。

- `threads_read_replies`: 返信・会話ツリー・自分の返信
- `threads_manage_replies`: 返信の非表示・再表示
- `threads_keyword_search`: TOP/RECENT、KEYWORD/TAG検索
- `threads_manage_mentions`: メンション取得
- `threads_delete`: 自分の投稿削除
- `threads_location_tagging`: 位置取得・位置タグ
- `threads_profile_discovery`: 公開プロフィールと投稿の発見

アプリをテストユーザー以外へ提供する場合、Meta管理画面でApp Reviewや高度なアクセスが必要になる可能性がある。必要性は管理画面の実際の権限表示で人間が確認する。

## C. 公式APIでは利用不可または確認できない

- 全Threads投稿の完全取得
- Threads全体の公式トレンドランキング
- おすすめアルゴリズムの内部情報
- プロフィール閲覧者の一覧
- 一般ユーザーの完全なフォロー一覧
- 他人の投稿の非公開Insights
- 個人単位のリンククリック情報
- いいね・フォロー操作の公式API
- Cookieを使う操作
- 公式エンドポイントを確認できない位置キーワード検索

これらをスクレイピングや非公式APIで補完しない。

## 実装順序

1. BotとThreadsタスクを停止し、完全バックアップを作成する。
2. 公式Postmanコレクションを調査し、機能対応表を作成する。
3. 共通APIクライアント、監査ログ、サーキットブレーカー、ページングを拡張する。
4. SQLiteへ同期・Insights・検索・分析・承認操作・レポート用テーブルを追加する。
5. 読み取り/分析CLIを実装する。
6. 人間承認必須の外部操作CLIと投稿形式のpreview/validate/publishを実装する。
7. Windowsタスク登録・開始・停止・状態確認用スクリプトだけを作る。登録・開始はしない。
8. 85シナリオ以上、既存全テスト、構文、PowerShell 5.1、完全dry-runを確認する。
9. 秘密情報を検査し、Draft PRまで公開する。本番タスクは停止したままにする。

## ロールアウト

- Phase A: 現在の権限でプロフィール、投稿、Insights、公開枠、ローカルレポートを検証。
- Phase B: 人間が追加スコープを再認証後、返信・メンション・検索を有効化。
- Phase C: 取得標本が十分になってから、相対トレンド・返信分析・X比較を評価。
- Phase D: 必要な投稿形式または外部操作だけを個別に有効化し、毎回 `--confirm` する。

本実装ではタスク登録、タスク開始、本番再起動、外部投稿を行わない。

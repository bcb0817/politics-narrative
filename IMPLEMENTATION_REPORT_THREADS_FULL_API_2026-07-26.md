# Threads API差分改修 実装報告書（2026-07-26）

1. **バックアップ**
   `D:\SNS Bot\politics-narrative-backup-20260726-031804` に、変更前のリポジトリ・`.git`・ローカル設定を含む完全バックアップを作成した。

2. **公式仕様の確認元**
   Meta公式Threads API Postman workspaceを基準に、プロフィール、投稿一覧、返信、メンション、インサイト、検索、公開、削除、再投稿、モデレーション、利用上限、OAuthを確認した。

3. **機能マトリクス**
   `docs/THREADS_API_CAPABILITY_MATRIX.md` に、公式提供、実装済み、権限待ち、未提供、推測実装禁止を区別して記録した。

4. **現在確認できた権限**
   `threads_basic`、`threads_content_publish`、`threads_manage_insights`。

5. **不足している権限**
   `threads_read_replies`、`threads_manage_replies`、`threads_keyword_search`、`threads_manage_mentions`、`threads_delete`、`threads_location_tagging`、`threads_profile_discovery`。

6. **OAuth追加権限**
   全機能分析用のスコーププロファイルを追加し、既存の最小スコープを維持した。

7. **App Review対象**
   現在付与されていない権限は、Meta側の製品設定、テスター設定、利用資格またはApp Reviewが必要になる可能性がある。

8. **公式APIに存在しない機能**
   フォロー操作など、公式仕様で確認できない操作は実装していない。代替としてプロフィール発見・分析のみを提供する。

9. **主要変更ファイル**
   `.env.example`、`local_bot.py`、`src/metrics_db.py`、`src/threads_api.py`、Threads関連ドキュメントとテストを更新した。

10. **主要追加ファイル**
    `src/threads_full_api.py`、設定2件、運用スクリプト6件、機能マトリクス、実装計画、新規テストを追加した。

11. **DBマイグレーション**
    Threadsプロフィール、投稿、返信、メンション、インサイト、検索、トレンド、API監査、操作監査、OAuth、保持管理用の加算的テーブルを追加した。

12. **投稿同期**
    `GET /me/threads` をページング・期間指定・重複排除付きで同期できる。

13. **投稿インサイト**
    投稿別インサイトを取得・保存し、取得不能な指標は捏造せず欠損として扱う。

14. **アカウントインサイト**
    アカウント指標と利用可能なフォロワー属性を取得・保存する。

15. **返信取得**
    会話返信と自分の返信を同期し、権限不足時は機能単位で安全に停止する。

16. **メンション取得**
    メンションを同期し、権限不足時は他機能を継続する。

17. **キーワード検索**
    政治用キーワード設定、キャッシュ、Post ID重複排除、取得上限を実装した。

18. **タグ検索**
    公式検索で利用可能なタグ検索モードを同じ安全制御下に置いた。

19. **トレンド分析**
    ローカルに取得した標本だけを用い、標本数を明示して傾向スコアを算出する。

20. **協調行動の兆候**
    断定を避け、時間集中、文面類似、アカウント分散などの観測可能な兆候だけをヒューリスティック評価する。

21. **返信分析**
    質問、賛否、注意語、反復をローカルルールで分類し、自動返信は行わない。

22. **X比較**
    同一コンテンツIDがある場合だけ比較し、指標定義が異なるため単純な勝敗を宣言しない。

23. **投稿フォーマット**
    TEXT、IMAGE、VIDEO、CAROUSELの検証・プレビュー・承認付き公開経路を実装した。

24. **引用投稿**
    引用元IDまたはURLを検証し、明示的な有効化と確認がある場合だけ実行する。

25. **返信投稿**
    返信先ID、権限、機能フラグ、`--confirm` を必須にした。

26. **再投稿**
    再投稿は専用フラグと確認を必須にし、書き込みリトライを禁止した。

27. **削除**
    削除は専用フラグと確認を必須にし、操作監査を保存する。

28. **返信モデレーション**
    hide/unhide等は権限・機能フラグ・確認を満たす場合だけ実行する。

29. **投票**
    公式仕様で利用できる場合だけ入力検証を通し、未対応レスポンス時は安全に失敗させる。

30. **位置情報**
    位置検索・タグ付けを権限分離し、位置IDを勝手に推測しない。

31. **Ghost Post**
    公式パラメータが利用できる場合に限り、プレビューと明示承認を経由する。

32. **利用上限**
    公式quota endpointを実装した。現時点のMeta応答はHTTP 500のため、外部書き込みを禁止するfail-safe結果を返す。

33. **データ保持**
    保持日数、削除要求、ユーザー単位の関連付け解除を実装した。

34. **Discord通知**
    結果要約だけを通知し、詳細ログ、秘密情報、トークン、認証コードは送信しない。

35. **Windows運用スクリプト**
    実行、登録、開始、停止、状態確認用PowerShellを追加した。今回の作業ではタスク登録・開始を行っていない。

36. **環境変数**
    `.env.example` と既存値を上書きしない追加スクリプトを整備した。実ファイルの秘密値はGit管理しない。

37. **API呼び出し見積り**
    読み取り回数は同期対象、ページ数、投稿数で変動するため、API監査テーブルの実測値を日次・週次報告へ集計する。

38. **OpenAI追加費用**
    新規分析はローカルルールが既定で、追加OpenAI費用は0ドル。将来LLM分類を有効化しても既存予算管理を経由する。

39. **既存テスト**
    既存機能を含む全592件を4分割して実行し、すべて成功した。全件一括実行は600秒の実行上限に達したため、同一テスト群を分割して完走させた。

40. **新規テスト**
    Threads関連テスト102件で、権限、リトライ、監査、DB、検索、分析、書き込みガード、保持処理を検証した。

41. **ドライラン**
    主要10コマンドを実行し、すべて正常終了した。ドライランの外部API呼び出し・書き込み・Discord送信は0件。

42. **外部変更の有無**
    今回の検証ではThreads投稿、返信、削除、モデレーション、Discord送信、Windowsタスク登録を行っていない。

43. **残存リスク**
    Meta側の権限未付与、quota endpointのHTTP 500、指標提供範囲のアカウント差、API仕様変更が残る。

44. **人手で必要なMeta作業**
    必要権限をThreads API設定へ追加し、テスターまたはApp Review条件を満たしたうえで再認証する。

45. **再認証手順**
    `python local_bot.py threads-oauth-start --scope-profile full-analysis` を実行し、ブラウザ認証後に `threads-permissions` で付与結果を確認する。

46. **段階導入**
    Phase 1は読み取りと分析、Phase 2は権限取得後の返信・メンション、Phase 3は人手承認付き書き込みの順とする。

47. **タスク登録コマンド**
    承認後に `powershell -ExecutionPolicy Bypass -File .\production\register_threads_analytics_tasks.ps1` を実行する。今回は未実行。

48. **Bot再起動コマンド**
    承認後に既存の本番起動手順を使用する。今回の改修では政治BotおよびThreads関連タスクを停止状態のまま維持する。

49. **ロールバック**
    コードはGitコミットを戻すか、完全バックアップ `D:\SNS Bot\politics-narrative-backup-20260726-031804` から復元できる。実データ復元前には対象パスを再確認する。

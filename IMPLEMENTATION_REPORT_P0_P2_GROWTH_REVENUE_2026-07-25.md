# P0・P1・P2 実装・検証報告 📊

## 結果

- 最新のローカル実装を正本として差分改修した。
- SQLiteの既存データを保持したまま、追加カラム・追加テーブルを移行した。
- Python構文検査に合格した。
- `unittest` 277件が全件合格した。
- `POST_ENABLED=false` の強制ドライランは終了コード0で終了し、X投稿は0件だった。
- コンテンツパイプラインはShorts下書き3件、note下書き2件を生成し、公開操作は0件だった。
- 本番タスクは再起動していない。

## P0：監査・安全性 🛡️

- xAI実費台帳の検証コマンド `verify-xai-ledger` を追加した。
- request ID、ticks換算、重複集計、キャッシュ課金、月境界、実費・推定費、
  tool-call数を検査する。
- 現行台帳は過去レコードのtool-call数不整合によりFAILであり、
  `XAI_COST_LEDGER_VERIFIED=false` 維持を推奨する。
- 有料探索は `xai` / `native_x` / `none` の排他設定にし、同一分の二重実行を防止した。
- 現在は `xai` が有効、Native X Searchは無効で、二重実行検出はない。
- 古いモデル移行スクリプトを `archive/deprecated/model_migrations/` に隔離した。
- 現行モデルキーのみを安全に変更する
  `production/set_current_openai_models.ps1` を追加した。
- 現行タスクを保護しつつ旧タスク候補だけを無効化する管理者用スクリプトを追加した。

## P1：品質・効果測定 📈

- 品質評価に合格率、失格理由、前回差、7日・30日推移を追加した。
- 112件のrule-only fixture評価を実行し、期待結果との一致は112/112だった。
  合格率14.29%は意図的な不合格fixtureを含むテスト集合の比率であり、
  実投稿の合格率を示すものではない。
- OpenAI利用を用途別に集計し、未知の旧集計データを `unknown` として可視化した。
- 手動引用・返信候補について、承認、手動投稿ID、結果CSV取込、効果比較を追加した。
  Xへの自動返信・自動引用は引き続き0件である。
- フォロワー増減を投稿前30分・投稿後120分の窓で推定し、
  投稿重複時は信頼度を下げる設計にした。
- 朝夕ダイジェスト比較と最小標本数10件の判定を追加した。

## P2：成長・収益導線 🌱

- note、YouTube、ニュースレター、購入のコンバージョン種別を追加した。
- イベントキーによる重複排除と、不正なイベント種別の隔離を追加した。
- 投稿種別・ダイジェスト種別・キャンペーン・コンテンツ別の分析を追加した。
- xAI ROIは、インプレッション単独では増額せず、プロフィールクリックまたは
  フォロー改善、品質・信頼、訂正・削除率、帰属コスト、最小標本数で判断する。
- 週次上位投稿からShorts最大3件、note最大2件をローカル下書き生成する。
  訂正・削除・強すぎる怒り・低信頼の投稿は素材から除外する。
- 外部公開、ブラウザ自動操作、YouTube/note API書込みは実装していない。

## 実データ診断

- 公開投稿: 68件
- OpenAI: 58 calls、約 `$0.8185`
- xAI: 10 requests、約 `$1.8926`
- X API推定: 約 `$0.3250`
- API合計: 約 `$3.0361`
- 月末予測: 約 `$5.3679` / 約886円
- 5,000円予算の予測残額: 約4,114円
- engagement、外部conversion、投稿帰属フォローは標本不足のため判定保留。

## 運用上の注意 ⚠️

- xAI台帳検証がPASSするまでは実効上限 `$2` を維持する。
- 成長指標は標本10件未満で勝者や増額を決定しない。
- `.env` の秘密情報は上書き・出力していない。
- 本番タスク停止は安全承認されなかったため実行していない。
  ユーザー指示どおり、本作業後の再起動も実施していない。
